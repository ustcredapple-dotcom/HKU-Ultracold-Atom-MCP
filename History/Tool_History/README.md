# Tool History

这里用于保存未来每个工具的前序版本或重要快照。

当前 `Worker/Artiq_Project` 是从 `Z:\Wang Junjie\Artiq_Project` 原样迁移的初始版本，尚未对工具代码做功能性修改，因此未额外复制一份 74 MB 的快照。

建议后续规则:

- 修改 `Worker` 中已有工具前，先把旧版本复制到本目录对应子目录。
- 快照文件名包含日期、工具名和修改原因。
- 不保存包含 token、密码或私有配置的 `.local` 文件。
